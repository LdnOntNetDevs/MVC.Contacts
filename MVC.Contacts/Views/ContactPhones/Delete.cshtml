@model ContactPhone

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Contact Phone</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.PhoneNumber)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.PhoneNumber)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Type)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Type)
        </dd>

    </dl>

    @using (Html.BeginForm(new { RedirectUrl = HttpContext.Current.Request.UrlReferrer.OriginalString })) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-default" /> |
            @if (HttpContext.Current.Request.UrlReferrer.OriginalString.Contains("/Edit/"))
            {
                @Html.ActionLink("Back to List", "Edit", "Contacts", new { id = Model.ContactId }, null)
            }
            else
            {
                @Html.ActionLink("Back to List", "Details", "Contacts", new { id = Model.ContactId }, null)
            }
        </div>
    }
</div>
